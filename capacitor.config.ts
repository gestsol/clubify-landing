import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Clubify Landing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
